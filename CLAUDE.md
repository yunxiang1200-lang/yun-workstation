# 项目记忆

- 本仓库即线上工作台源码，线上地址：https://yunxiang1200-lang.github.io/yun-workstation/
- 修改本站内容 = 修改本仓库文件（主要是 `index.html` 与 `study/` 目录），然后 commit 并 push 到 `origin master`，GitHub Pages 会自动部署。
- 不要只改本地而不推送；改完后需 `git push origin master` 才能让线上生效。
